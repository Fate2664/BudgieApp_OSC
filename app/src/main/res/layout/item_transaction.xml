<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingStart="10dp"
    android:paddingEnd="10dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:layout_marginBottom="10dp"
    app:cardBackgroundColor="@color/white"
    app:cardCornerRadius="14dp"
    app:cardElevation="1dp"
    app:strokeColor="@color/black"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="80dp"
        android:padding="10dp">

        <!-- Date -->
        <TextView
            android:id="@+id/txtTransactionDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:textColor="@color/black_transparent"
            android:textSize="17sp"
            android:textStyle="bold"
            android:text="29/07/2026"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Category -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardTransactionCategory"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:layout_marginStart="40dp"
            android:minWidth="100dp"
            app:cardBackgroundColor="@color/orange"
            app:cardCornerRadius="10dp"
            app:cardElevation="2dp"
            app:layout_constraintStart_toEndOf="@id/txtTransactionDate"
            app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/txtTransactionCategory"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center"
            android:layout_gravity="center"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:text="Food"
            android:textColor="@color/black_transparent"
            android:textSize="15sp"
            android:textStyle="bold" />

        </com.google.android.material.card.MaterialCardView>

        <!-- Amount -->
        <TextView
            android:id="@+id/txtTransactionAmount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="15dp"
            android:textColor="@color/black_transparent"
            android:textSize="18sp"
            android:textStyle="bold"
            android:text="R200.00"
            app:layout_constraintStart_toEndOf="@id/cardTransactionCategory"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Payee -->
        <TextView
            android:id="@+id/txtTransactionPayee"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_marginTop="6dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/black_transparent"
            android:textSize="15sp"
            android:text="Woolworths"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="@+id/cardTransactionCategory"
            app:layout_constraintEnd_toEndOf="@+id/cardTransactionCategory"
            app:layout_constraintTop_toBottomOf="@+id/cardTransactionCategory" />

        <!-- Attachment -->
        <ImageButton
            android:id="@+id/btnAttachment"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Open Attachment"
            android:padding="8dp"
            app:tint="@color/black"
            android:visibility="visible"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:src="@android:drawable/ic_menu_gallery" />


    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>